/**
 * create virtual environment
 * @param python interpreter for virtual environment
 * @param envdir target directory to create virtual environment
 */
 def createVirtualEnv(python, envdir) {
     //create virtual env
 }

/**
 * Execute bat command inside virtual environment
 * @param cmd Bat command to execute within virtual environment
 */
def execWin(String cmd){
    //execute cmd in virtual env
}

return this
